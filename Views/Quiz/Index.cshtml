@model IEnumerable<QuizApplication.Models.Quiz>

<h2>Available Quiz Topics</h2>

<ul>
    @foreach (var quiz in Model)
    {
        <li>
            <a asp-action="StartQuiz" asp-route-id="@quiz.Id">@quiz.Topic</a>
        </li>
    }
</ul>

